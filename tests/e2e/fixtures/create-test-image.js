/**
 * Creates a simple test PNG image for E2E testing
 * Run with: node tests/e2e/fixtures/create-test-image.js
 */

const fs = require('fs');
const path = require('path');

// Simple 100x100 blue PNG
// This is a minimal valid PNG file
const pngData = Buffer.from([
  0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a, // PNG signature
  0x00, 0x00, 0x00, 0x0d, 0x49, 0x48, 0x44, 0x52, // IHDR chunk
  0x00, 0x00, 0x00, 0x64, 0x00, 0x00, 0x00, 0x64, // 100x100
  0x08, 0x02, 0x00, 0x00, 0x00, 0xff, 0x80, 0x02, // 8-bit RGB
  0x03, 0x00, 0x00, 0x00, 0x01, 0x73, 0x52, 0x47, // sRGB chunk
  0x42, 0x00, 0xae, 0xce, 0x1c, 0xe9, 0x00, 0x00, // sRGB data
  0x00, 0x00, 0x49, 0x45, 0x4e, 0x44, 0xae, 0x42, // IEND chunk
  0x60, 0x82
]);

const outputPath = path.join(__dirname, 'test-image.png');

// For now, create a simple placeholder file
// In production, use a proper image library
fs.writeFileSync(outputPath, pngData);
console.log(`Created test image at: ${outputPath}`);
